# دليل تصميم واجهات المستخدم
## الجزء الثاني: المكونات التفاعلية و User Flows

---

# 3. المكونات التفاعلية (Interactive Components)

## 3.1 Score Gauge Component (مقياس الدرجات)

### **Visual Design:**

```
┌────────────────────────────────┐
│      النضج الرقمي              │
│                                │
│        ┌─────────┐             │
│       ╱           ╲            │
│      │             │           │
│      │      45     │           │
│      │             │           │
│       ╲           ╱            │
│        └─────────┘             │
│                                │
│        Developing              │
│                                │
│   [████████░░░░░░░░]          │
│   25 ────── 50 ────── 75      │
│                                │
└────────────────────────────────┘
```

### **Component Props:**

```typescript
interface ScoreGaugeProps {
  title: string;
  score: number;        // 0-100
  level: string;        // 'beginner' | 'developing' | 'advanced' | 'expert'
  color?: string;       // primary, success, warning, error
  size?: 'small' | 'medium' | 'large';
  showProgress?: boolean;
  benchmarkScore?: number;
}

// Usage Example:
<ScoreGauge
  title="النضج الرقمي"
  score={45}
  level="developing"
  color="primary"
  size="medium"
  showProgress={true}
  benchmarkScore={60}
/>
```

### **Color Coding:**

```css
/* Score Ranges */
.score-0-25   { color: #d32f2f; }  /* Red - Beginner */
.score-26-50  { color: #ed6c02; }  /* Orange - Developing */
.score-51-75  { color: #1976d2; }  /* Blue - Advanced */
.score-76-100 { color: #2e7d32; }  /* Green - Expert */
```

---

## 3.2 Alert Card Component (بطاقة التنبيه)

### **Visual Design:**

```
┌──────────────────────────────────────────────────────┐
│  ⚠️  ميزانية تسويقية منخفضة                  [×]   │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ميزانيتك التسويقية 6% من الإيرادات - المعيار     │
│  للقطاع التعليمي هو 10-15%                         │
│                                                      │
│  💡 التوصية:                                        │
│  زيادة الميزانية تدريجياً للاستفادة من فرص النمو   │
│                                                      │
│  الخبير: خبير التحليل المالي                       │
│  الأولوية: عالية                                   │
│                                                      │
│  [عرض التفاصيل]  [تجاهل]                           │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### **Alert Types:**

```typescript
type AlertType = 'critical' | 'high' | 'warning' | 'info' | 'opportunity';

interface AlertCardProps {
  type: AlertType;
  severity: number;      // 1-10
  title: string;
  message: string;
  recommendation?: string;
  expert?: string;
  priority?: 'low' | 'medium' | 'high' | 'critical';
  onDismiss?: () => void;
  onViewDetails?: () => void;
}
```

### **Color & Icon Mapping:**

```css
/* Critical Alert */
.alert-critical {
  background: #ffebee;
  border-left: 4px solid #d32f2f;
  icon: ⛔
}

/* High Priority Alert */
.alert-high {
  background: #fff3e0;
  border-left: 4px solid #ff6f00;
  icon: ⚠️
}

/* Warning Alert */
.alert-warning {
  background: #fff9c4;
  border-left: 4px solid #f57f17;
  icon: ⚡
}

/* Info Alert */
.alert-info {
  background: #e3f2fd;
  border-left: 4px solid #1976d2;
  icon: ℹ️
}

/* Opportunity Alert */
.alert-opportunity {
  background: #e8f5e9;
  border-left: 4px solid #2e7d32;
  icon: 💡
}
```

---

## 3.3 Expert Insight Card (بطاقة رؤية الخبير)

### **Visual Design:**

```
┌────────────────────────────────────────────────────┐
│  👨‍💼 خبير التحليل المالي                           │
│                                                    │
│  التقييم:                                          │
│  صحة مالية جيدة مع هوامش ربح 25%                  │
│                                                    │
│  النتائج الرئيسية:                                 │
│  ✓ الإيرادات في نمو 15% سنوياً                    │
│  ⚠ الميزانية التسويقية 6% من الإيرادات           │
│                                                    │
│  التوصيات:                                         │
│  1️⃣ زيادة الميزانية التسويقية إلى 10-12%         │
│     العائد المتوقع: 4-6x                           │
│                                                    │
│  2️⃣ تحسين كفاءة الإنفاق التسويقي                  │
│     التركيز على القنوات عالية العائد              │
│                                                    │
│  [عرض التحليل الكامل]                             │
│                                                    │
│  الثقة: ████████░ 85%                             │
└────────────────────────────────────────────────────┘
```

### **Component Structure:**

```typescript
interface ExpertInsight {
  expert_id: string;
  expert_name: string;
  expert_icon: string;
  assessment: string;
  key_findings: Finding[];
  recommendations: Recommendation[];
  confidence_score: number;
}

interface Finding {
  type: 'positive' | 'negative' | 'neutral';
  message: string;
  icon: string;
}

interface Recommendation {
  priority: number;
  action: string;
  expected_impact?: string;
  expected_roi?: string;
  timeline?: string;
}
```

---

## 3.4 Progress Stepper (مؤشر التقدم خطوة بخطوة)

### **Visual Design:**

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  ●━━━━●━━━━●━━━━○━━━━○                                  │
│  ✓    ✓    ▶    ○    ○                                  │
│  معلومات  مالية  تسويق  عملاء  منافسة                 │
│  أساسية                                                 │
│                                                         │
│  الخطوة 3 من 5: قسم التسويق                            │
│  التقدم الإجمالي: 47%                                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### **States:**

```css
/* Completed Step */
.step-completed {
  background: #2e7d32;
  icon: ✓
  color: white;
}

/* Active Step */
.step-active {
  background: #1976d2;
  icon: ▶
  color: white;
  animation: pulse;
}

/* Upcoming Step */
.step-upcoming {
  background: #e0e0e0;
  icon: ○
  color: #9e9e9e;
}
```

---

## 3.5 Recommendation Card (بطاقة توصية)

### **Visual Design:**

```
┌────────────────────────────────────────────────────┐
│  1️⃣  بناء حضور قوي على Instagram                   │
│      الأولوية: عالية                              │
├────────────────────────────────────────────────────┤
│                                                    │
│  📋 الإجراءات:                                     │
│  □ إنشاء حساب أعمال احترافي                       │
│  □ تصوير محتوى عالي الجودة                        │
│  □ نشر 3-4 منشورات أسبوعياً                       │
│  □ التفاعل مع المتابعين                           │
│                                                    │
│  💰 الاستثمار المطلوب: 15,000 ريال/شهر            │
│  📈 الأثر المتوقع: 200-300 عميل جديد/شهر          │
│  ⏱ الإطار الزمني: 3 أشهر                          │
│  ✨ العائد المتوقع: 4-6x                           │
│                                                    │
│  [ابدأ التنفيذ]  [مزيد من التفاصيل]              │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

# 4. تدفقات المستخدم (User Flows)

## 4.1 Complete Assessment Flow

```
┌─────────────┐
│   الدخول    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ لوحة التحكم  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ ابدأ تقييم  │
│    جديد     │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  اختر نوع   │
│  التقييم    │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│ معلومات     ├──────►│  حفظ مسودة  │
│ الشركة      │      └──────────────┘
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   الأسئلة   │◄─────┐
│   الذكية    │      │
└──────┬──────┘      │
       │             │
       │ ◄───────────┘
       │  (أسئلة متعددة)
       │
       ▼
┌─────────────┐
│   مراجعة    │
│  الإجابات   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   إرسال     │
│  للتحليل    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  التحليل    │
│   (تحميل)   │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│   نتائج     ├──────►│ عرض التقرير │
│  التحليل    │      └──────────────┘
└──────┬──────┘              │
       │                     │
       │                     ▼
       │              ┌──────────────┐
       │              │ تحميل PDF   │
       │              └──────────────┘
       │
       ▼
┌─────────────┐
│   حفظ في    │
│  Dashboard  │
└─────────────┘
```

## 4.2 Question Navigation Flow

```
    ┌─────────────┐
    │  السؤال 1   │
    └──────┬──────┘
           │
    ┌──────▼──────┐
    │  الإجابة   │
    └──────┬──────┘
           │
    ┌──────▼──────┐
    │  التحقق من  │
    │  الإجابة    │
    └──────┬──────┘
           │
     ┌─────┴──────┐
     │            │
 ┌───▼───┐   ┌───▼────┐
 │ صحيحة │   │ خاطئة  │
 └───┬───┘   └───┬────┘
     │           │
     │      ┌────▼────┐
     │      │  رسالة  │
     │      │  خطأ    │
     │      └────┬────┘
     │           │
     │      ◄────┘
     │
┌────▼────┐
│ حفظ في  │
│القاعدة  │
└────┬────┘
     │
┌────▼────────┐
│ تحليل فوري  │
│ للإجابة     │
└────┬────────┘
     │
┌────▼─────────┐
│ كشف الأنماط  │
│ والعلاقات    │
└────┬─────────┘
     │
     ▼
 ┌───────────┐
 │ التنبيهات؟│
 └───┬───┬───┘
     │   │
 نعم │   │ لا
     │   │
 ┌───▼───┐ │
 │ عرض   │ │
 │التنبيه│ │
 └───┬───┘ │
     │     │
     └──┬──┘
        │
   ┌────▼─────┐
   │ السؤال   │
   │ التالي؟  │
   └────┬─────┘
        │
    ┌───┴────┐
    │        │
  نعم│        │لا
    │        │
┌───▼───┐ ┌──▼─────┐
│السؤال │ │ إكمال  │
│التالي │ │التقييم │
└───────┘ └────────┘
```

---

# 5. Responsive Design (التصميم المتجاوب)

## 5.1 Breakpoints

```css
/* Extra Small Devices (Phones) */
@media (max-width: 599px) {
  /* Mobile First Design */
}

/* Small Devices (Tablets) */
@media (min-width: 600px) and (max-width: 959px) {
  /* Tablet Layout */
}

/* Medium Devices (Laptops) */
@media (min-width: 960px) and (max-width: 1279px) {
  /* Desktop Layout */
}

/* Large Devices (Desktops) */
@media (min-width: 1280px) and (max-width: 1919px) {
  /* Large Desktop */
}

/* Extra Large Devices */
@media (min-width: 1920px) {
  /* XL Desktop */
}
```

## 5.2 Mobile Layout Adjustments

### **Dashboard on Mobile:**

```
┌──────────────────┐
│  ☰  Marketing AI │
├──────────────────┤
│                  │
│  مرحباً خالد 👋   │
│                  │
│ ┌──────────────┐ │
│ │  📊          │ │
│ │   12         │ │
│ │ التقييمات    │ │
│ └──────────────┘ │
│                  │
│ ┌──────────────┐ │
│ │  📝          │ │
│ │   8          │ │
│ │ التقارير     │ │
│ └──────────────┘ │
│                  │
│ ┌──────────────┐ │
│ │  ⚠️          │ │
│ │   3          │ │
│ │ التنبيهات    │ │
│ └──────────────┘ │
│                  │
│ ┌──────────────┐ │
│ │ ابدأ تقييم   │ │
│ │   جديد       │ │
│ └──────────────┘ │
│                  │
└──────────────────┘
```

---

# 6. Animations & Transitions (الحركات والانتقالات)

## 6.1 Loading States

```css
/* Spinner Animation */
@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-spinner {
  animation: spin 1s linear infinite;
}

/* Skeleton Loading */
@keyframes skeleton-loading {
  0% { background-position: -200px 0; }
  100% { background-position: calc(200px + 100%) 0; }
}

.skeleton {
  background: linear-gradient(90deg, 
    #f0f0f0 0px, 
    #e0e0e0 40px, 
    #f0f0f0 80px
  );
  background-size: 200px;
  animation: skeleton-loading 1.4s ease-in-out infinite;
}
```

## 6.2 Page Transitions

```css
/* Fade In */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.page-enter {
  animation: fadeIn 0.3s ease-in;
}

/* Slide In From Right */
@keyframes slideInRight {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.card-enter {
  animation: slideInRight 0.4s ease-out;
}
```

## 6.3 Micro-interactions

```css
/* Button Hover */
.button {
  transition: all 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 8px rgba(0,0,0,0.15);
}

.button:active {
  transform: translateY(0);
}

/* Card Hover */
.card {
  transition: transform 0.2s, box-shadow 0.2s;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.1);
}

/* Success Animation */
@keyframes checkmark {
  0% {
    transform: scale(0);
    opacity: 0;
  }
  50% {
    transform: scale(1.2);
    opacity: 1;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

.success-icon {
  animation: checkmark 0.5s ease;
}
```

---

# 7. Accessibility (إمكانية الوصول)

## 7.1 ARIA Labels

```html
<!-- Button with aria-label -->
<button aria-label="ابدأ تقييم جديد">
  <AddIcon />
</button>

<!-- Form input with aria-describedby -->
<input 
  type="email"
  aria-label="البريد الإلكتروني"
  aria-describedby="email-help"
  aria-required="true"
/>
<span id="email-help">
  أدخل بريدك الإلكتروني الصحيح
</span>

<!-- Alert with role -->
<div role="alert" aria-live="polite">
  تم حفظ التغييرات بنجاح
</div>
```

## 7.2 Keyboard Navigation

```typescript
// Handle keyboard navigation
const handleKeyDown = (e: React.KeyboardEvent) => {
  switch(e.key) {
    case 'Enter':
      handleSubmit();
      break;
    case 'Escape':
      handleCancel();
      break;
    case 'ArrowRight':
      nextQuestion();
      break;
    case 'ArrowLeft':
      previousQuestion();
      break;
  }
};
```

## 7.3 Focus Management

```css
/* Focus Visible */
*:focus-visible {
  outline: 2px solid #1976d2;
  outline-offset: 2px;
}

/* Skip to content */
.skip-to-content {
  position: absolute;
  top: -40px;
  left: 0;
  background: #1976d2;
  color: white;
  padding: 8px;
  text-decoration: none;
}

.skip-to-content:focus {
  top: 0;
}
```

---

**يتبع: الملف الأخير مع دليل التطوير الكامل وأمثلة إضافية...**
